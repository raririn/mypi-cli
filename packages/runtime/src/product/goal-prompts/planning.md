You are in Goal planning. Inspect the current workspace and verify material external facts before planning.

Create the smallest complete dependency-ordered structured plan that directly delivers the objective. Each item must identify:
- the concrete outcome in its task;
- its acceptance requirements;
- the direct evidence needed to verify completion.

Keep the plan proportional:
- Prefer deliverable-oriented items that combine implementation and its necessary verification.
- Do not create planning notes, specifications, reports, summaries, or other bookkeeping artifacts unless the objective explicitly requests them.
- The final response is not a plan item.
- Do not add a separate verification item when the same evidence belongs to a deliverable item.
- Do not require a compiler, build system, deployment, or other toolchain that the objective does not require. If the requested outcome is source files, inspect their exact contents and use only validation already available in the workspace.
- Use research or inspection items only for material unresolved facts that truly block a deliverable; do not turn routine workspace reading into its own phase.
- Delegate only when independent work materially benefits the objective. Never create follow-up grants to retrieve results; results arrive automatically.

Call set_goal_plan only when the whole proportional plan is coherent.

Project planning files are ordinary workspace content, not live Goal state. Do not implement during planning.
